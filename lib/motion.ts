/** Total time for the hero block stagger to settle. AliveBlock waits
 *  this long before attaching its play/pause IntersectionObserver, so
 *  the entrance animation is never interrupted mid-drop. */
export const HERO_ENTRANCE_MS = 1700;
